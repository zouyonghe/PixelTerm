# PixelTerm - 终端图片浏览器

一个基于chafa的现代化终端图片浏览器，支持在终端中浏览和查看图片。

## 功能特性

- 🖼️ 支持多种图片格式 (JPG, PNG, GIF, BMP, WebP, TIFF)
- 📁 目录浏览功能
- 🔍 缩放和适配显示
- ⌨️ 键盘导航
- 🎨 自适应终端大小
- ⚙️ 可配置的显示选项
- 📋 文件列表显示

## 依赖

- Python 3.7+
- chafa 命令行工具
- Pillow Python库

## 安装

### 1. 安装chafa

```bash
# Arch Linux
sudo pacman -S chafa

# Ubuntu/Debian
sudo apt-get install chafa

# macOS
brew install chafa

# 其他系统请参考 https://hpjansson.org/chafa/
```

### 2. 安装Python依赖

```bash
pip install -r requirements.txt
```

### 3. 给脚本添加执行权限

```bash
chmod +x pixelterm.py
```

## 使用

### 基本用法

```bash
# 浏览当前目录的图片
python pixelterm.py

# 浏览指定目录的图片
python pixelterm.py /path/to/images

# 或者直接运行
./pixelterm.py /path/to/images
```

### 快捷键

| 按键 | 功能 |
|------|------|
| ←/→ | 上一张/下一张图片 |
| ↑/↓ | 在文件列表中导航 |
| +/- | 放大/缩小 |
| r | 重置缩放 |
| h/? | 显示帮助 |
| q | 退出 |
| u | 返回上级目录 |
| l | 显示子目录列表 |
| R | 刷新 |

### 配置

配置文件保存在 `~/.pixelterm/config.json`，包含以下选项：

- 显示设置 (缩放、适配等)
- chafa参数
- 界面选项
- 导航设置

## 示例

```bash
# 启动图片浏览器
./pixelterm.py ~/Pictures

# 浏览当前目录
./pixelterm.py .
```

## 项目结构

```
PixelTerm/
├── pixelterm.py       # 主程序
├── image_viewer.py    # 图片显示模块
├── file_browser.py    # 文件浏览器模块
├── interface.py       # 用户界面模块
├── config.py          # 配置管理模块
├── requirements.txt   # Python依赖
├── pyproject.toml     # 项目配置
└── README.md          # 项目说明
```

## 许可证

MIT License